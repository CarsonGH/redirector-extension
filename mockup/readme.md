# mockup

This is just so you can use live-server to screenshot the interface as its annoying to screenshot actual extension popups as they go away whenever  you try to open screenshot/dev tools cant access them easily.